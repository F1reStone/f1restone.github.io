# Local icons

This tracked directory is the default local SVG source for `astro-icon`.
The site currently uses the installed Lucide and Simple Icons collections.
Keep this file so fresh checkouts include the directory and the integration can
complete icon loading and type generation. Add custom `.svg` icons here if needed.
